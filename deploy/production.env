# Production env for EC2 public hosting
# Use port 80 (via nginx) so WhatsApp detects the registration link.
NODE_ENV=production
PORT=3000
PUBLIC_URL=http://3.16.112.125
ADMIN_REG_EMAIL=omkarsinha4@gmail.com
MAIL_FROM=ArcusVerse <noreply@arcusverse.local>
# Configure SMTP to enable outbound email (Gmail app password recommended):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=omkarsinha4@gmail.com
# SMTP_PASS=your-app-password
# MAIL_FROM="ArcusVerse <omkarsinha4@gmail.com>"
