# Production env for EC2 public hosting
NODE_ENV=production
PORT=3000
# Use port 80 (nginx) so WhatsApp detects registration links
PUBLIC_URL=http://3.16.112.125
ADMIN_REG_EMAIL=omkarsinha4@gmail.com
MAIL_FROM=ArcusVerse <omkarsinha4@gmail.com>

# Required for real outbound email (without these, emails only log as dry-run):
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=omkarsinha4@gmail.com
# Set SMTP_PASS to a Gmail App Password (Google Account → Security → App passwords)
# SMTP_PASS=
